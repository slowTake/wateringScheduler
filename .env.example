# Supabase
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Public app URL for Supabase auth email links (reset password, email confirm).
# Must match Supabase Auth → URL Configuration (Site URL + Redirect URLs).
VITE_APP_URL=

# Discord bot plant-watering webhook (optional — in-app notifications still work without these)
DISCORD_BOT_WEBHOOK_URL=
PLANT_WEBHOOK_SECRET=
